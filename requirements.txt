# EduTGT 依赖（Python 3.8+）
# 安装：pip install -r requirements.txt
# 若需 GPU，请先按 https://pytorch.org 安装对应版本的 torch，再执行上式。

torch>=1.9.0
numpy>=1.19.0
pandas>=1.2.0
scipy>=1.6.0
scikit-learn>=0.24.0
tqdm>=4.60.0
xgboost>=1.4.0

# 可选：链路预测基线 GraphSAGE 需要 torch-geometric；TGAT、JODIE 仅需 torch。
# 若需 GraphSAGE：pip install torch-geometric（不安装时 EduTGT、main_link、TGAT、JODIE、passfail 仍可运行）。
